apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service:21-deployment
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: auth-service:21
  template:
    metadata:
      labels:
        app: auth-service:21
    spec:
      containers:
        - name: auth-service:21
          image: aishjp/auth-service:21:
          ports:
            - containerPort: 8082
          resources:
            requests:
              cpu: "100m"
            limits:
              cpu: "500m"
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: prod
