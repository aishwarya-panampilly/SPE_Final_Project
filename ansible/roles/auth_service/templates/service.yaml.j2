apiVersion: v1
kind: Service
metadata:
  name: {{ image_name }}
spec:
  selector:
    app: {{ image_name }}
  ports:
    - protocol: TCP
      port: {{ container_port }}
      targetPort: {{ container_port }}
  type: ClusterIP
